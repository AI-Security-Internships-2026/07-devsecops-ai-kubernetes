[pytest]
testpaths = tests
# Unit tests are offline and deterministic: no network, no Trivy, no cluster, no LLM.
# Anything needing real infrastructure is marked `integration` and opted into
# explicitly with `pytest -m integration`.
addopts = -m "not integration" -q
markers =
    integration: needs real infrastructure (cluster, Trivy, network)
